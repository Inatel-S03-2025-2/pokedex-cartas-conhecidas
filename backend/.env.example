# Database
# For development you can use SQLite (no external DB server required):
# This will create a local file `dev.db` inside the `backend` folder.
DATABASE_URL="file:./dev.db"

# If you prefer PostgreSQL locally, uncomment and configure the line below and
# set DATABASE_URL accordingly in your `.env` file for that environment.
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pokedex_db?schema=public"

# JWT Secret
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Server
PORT=3000
NODE_ENV=development